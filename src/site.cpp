#include "site.hpp"

#include <iostream>

namespace MMaze {
    Site::Site(unsigned int l, unsigned int c) : Case(l, c) {

    }
    
    Site::Site(unsigned int index) : Case(index) {
        
    }
}